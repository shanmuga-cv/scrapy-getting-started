project can be created using `scrapy startproject olx`.
Haveing a project creates the skeletal code which is easier to maintain and organize multiple spiders.


run_command: `scrapy crawl olx_cars -t csv -o olx_cars.csv` and `scrapy crawl olx_bikes -t csv -o olx_bikes.csv`