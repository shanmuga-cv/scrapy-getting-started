Haveing items defines a set of fields and additional rules for each fields. 
This helps in reduced programming errors and typo

run_command: `scrapy crawl olx_cars -t csv -o olx_cars.csv` and `scrapy crawl olx_bikes -t csv -o olx_bikes.csv`